# zsh

The private source repository contains a machine-specific `.zshrc` with local
automation, absolute paths, and API-related environment wiring.

It is intentionally not exported into this public repository. Keep your public
zsh profile small, or maintain it as a separate public-safe file after reviewing
each line.
